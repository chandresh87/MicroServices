spring:
  application:
    name: organizationService
  profiles:
    active:
    - default
  cloud:
    config:
      uri: http://localhost:8888
      username: config-user
      password: config-pass